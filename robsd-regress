#!/bin/ksh

set -eu

usage() {
	echo "usage: robsd-regress" 1>&2
	exit 1
}

. "${EXECDIR:-/usr/local/libexec/robsd}/util.sh"

[ $# -ne 0 ] && usage

setprogname "robsd-regress"
[ "$(id -u)" -ne 0 ] && fatal "must be run as root"
config_load "${ROBSDRC:-/etc/robsdrc}"
